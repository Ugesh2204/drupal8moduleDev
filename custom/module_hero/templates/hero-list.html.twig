{{ attach_library('module_hero/hero-style') }}
<h4>{{ title }}</h4>
<ol>
  {% for item in items %}
    <li class="animated {{ random(['bounce', 'flash', 'shake', 'jello']) }}">{{ item.name }}</li>
  {% endfor %}
</ol>
<strong>This is coming from our custom twig file.</strong>
